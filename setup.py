from setuptools import setup, find_packages

setup(
    name='mxnext',
    version='0.0.1',
    author='TuSimple',
    description='a simple wrapper for mxnet',
    install_requires=[],
    packages=find_packages(),
)
